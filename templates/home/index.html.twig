{% extends 'base.html.twig' %}

{% block title %}Movies
{% endblock %}

{% block body %}

{% block stylesheets %}
	<link rel="stylesheet" href="{{ asset('assets/css/pico.min.css') }}">
	<link rel="stylesheet" href="{{ asset('assets/css/home.css') }}">
	<script src="{{asset('assets/js/home.js')}}" defer></script>
{% endblock %}
<style>
	{# .example-wrapper {
		margin: 1em auto;
		max-width: 800px;
		width: 95%;
		font: 18px / 1.5 sans-serif;
	}
	.example-wrapper code {
		background: #F5F5F5;
		padding: 2px 6px;
	} #}
</style>



	<h1>Movies
		{{ controller_name }}!</h1>
	<div class="filmContainer" data-jsFilmFantasy={{jsFilmFantasy}}>
		{% for film in filmsGenresAction %}
<a href="{{ path('app_film', {'id':film.id})}}">

			<div class="film"> {{ film.title }} <br> <img src="{{ asset(film.urlImg) }}" alt=""> <br> {{film.genres}}
			</div> {% else %} <tr> <td colspan="12"> records found</td> </tr> {% endfor %} </div>

</a>
<div>

<h2>Top Fantasy</h2>

<div class="fantasy">
	{% for film in filmsGenresFantasy %}
	
	<div class="film"> {{ film.title }} <br> <img src="{{ asset(film.urlImg) }}" alt=""> <br>
	{{film.genres}} </div> {% else %} <tr> <td colspan="12"> records found</td> </tr> {% endfor %} </div> {% endblock %}

	